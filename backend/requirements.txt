fastapi==0.115.0
uvicorn[standard]==0.30.0
openai==1.40.0
httpx==0.27.0
web3==7.0.0
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.2
pydantic-settings==2.4.0
python-dotenv==1.0.1
bcrypt==4.2.0
PyJWT==2.9.0
# Cognee memory runs in a separate service (memory-sidecar) to keep these deps isolated;
# the backend only talks to it over HTTP via httpx (already a dependency above).
