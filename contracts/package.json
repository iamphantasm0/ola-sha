{
  "name": "ola-contracts",
  "version": "1.0.0",
  "description": "0G Chain OrderRegistry for Ola — settlement log",
  "license": "BUSL-1.1",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network og_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network og_mainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
